<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:theme="@style/Theme.Quizletandroid.AppWidgetContainerParent"
    android:background="@drawable/bg_widget"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView
        android:layout_gravity="start|center_vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/bg_shape_q_logo_vcut"
        android:scaleType="fitStart"
        android:tint="@color/widget_background_logo"
        android:contentDescription="@string/q_logo"/>
    <ProgressBar
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:foregroundTint="@color/ref_color_twilight500"/>
</FrameLayout>
